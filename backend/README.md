# Hospital Management System Backend

Production-oriented Node.js, Express, MySQL, JWT, bcrypt, Socket.IO, and Swagger backend.

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

Swagger is available at:

```txt
http://localhost:5000/api/docs
```

Health check:

```txt
GET /health
```
